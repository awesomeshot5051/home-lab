[Service]
# Allow Caddy to write to your MergedStorage mount
ReadWritePaths=/mnt/MergedStorage

# If strict sandboxing is on, you might also need this to see /mnt at all:
ProtectSystem=full
ProtectHome=true
PrivateTmp=true
